<!-- 
    EJS (Embedded JavaScript) é uma linguagem de template para gerar HTML com JavaScript no Node.js. 
    Ele permite a inclusão de código JavaScript dentro de arquivos de template, facilitando a criação 
    de páginas web dinâmicas.
-->

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
    </head>
    <body>
        <h1>Renderizando HTML dinâmico com EJS</h1>
        <!--         <p id="<%= 'teste' +' _ '+ (3 * 4)  %>">
            Texto qualquer <% console.log('teste' + '_' + (3 * 4)) %>
        </p> -->

        <p><%= message %></p>
        <% if(1 === 1) { %>
        <p>verdadeiro</p>
        <% } else {%>
        <p>falso</p>
        <% } %>
        <ul>
            <% for( let index = 0; index <= 10; index++ ){ %>
            <li>4 * <%= index %> = <%= (4 * index) %></li>
            <% } %>
        </ul>
    </body>
</html>
